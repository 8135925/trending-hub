# GitHub Personal Access Token（可选，但建议配置）
# 作用：将 GitHub API 限流额度从匿名 10 次/分钟提升到认证 30 次/分钟。
# 本站有 30 分钟数据缓存，不配置也能正常工作。
#
# 申请方式（fine-grained PAT，public repo 只读即可）：
#   GitHub -> Settings -> Developer settings -> Personal access tokens
#   -> Fine-grained tokens -> Generate new token
#   Repository access 选 Public repositories，权限无需勾选任何一项。
#
# 注意：该变量仅服务端使用，严禁加 NEXT_PUBLIC_ 前缀。
GITHUB_TOKEN=

# 智谱 AI 开放平台 API Key（可选，用于项目简介的大模型中文总结）
# 模型：GLM-4-Flash（永久免费），国内直连
# 申请：https://bigmodel.cn → 注册 → 右上角"API Keys"→ 创建
# 未配置时项目简介显示英文原文
ZHIPU_API_KEY=

# 可选：指定其他模型（默认 glm-4-flash；如 glm-4.7-flash，但新模型限流更严）
# ZHIPU_MODEL=
